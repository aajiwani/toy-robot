import CommandTupple from "./CommandTupple";

export default class {
	/**
	 * Executes the command and returns the new CommandTupple
	 * @param {CommandTupple} commandTupple
	 *
	 * @returns {CommandTupple}
	 */
	static Execute(commandTupple) {}
}
