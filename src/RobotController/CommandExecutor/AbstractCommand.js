/**
 * An Abstract interface for the command to be parsed and executed
 */
export default class {
	/**
	 * A method to facilitate the execution of command via text
	 * @param {string} commandText Commands coming in from user
	 * @param {RobotController} controller Instance controlling robot
	 */
	ParseAndExecute(commandText, controller) {}
}
